import { Feature1Directive } from './feature-1.directive';

describe('Feature1Directive', () => {
  it('should create an instance', () => {
    const directive = new Feature1Directive();
    expect(directive).toBeTruthy();
  });
});
